// Shared cart/checkout pricing constants — keep cart.tsx and checkout/index.tsx
// in agreement so the total a user previews in the cart matches what
// checkout actually charges.
export const FREE_DELIVERY_THRESHOLD = 500;
export const BASE_DELIVERY_CHARGE = 30;
export const PACKAGING_CHARGE = 10;
export const GST_RATE = 0.05; // 5%, applied to item total
