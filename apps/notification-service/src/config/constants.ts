/** Named constants for the notification service */

/** Time (in seconds) the OTP status key is kept in Redis */
export const OTP_STATUS_TTL_SECONDS = 120;

/** Maximum number of notifications returned per user query */
export const MAX_NOTIFICATIONS_PER_PAGE = 50;
