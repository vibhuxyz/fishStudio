{
  "name": "@repo/db",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "prisma:generate": "bunx prisma generate",
    "prisma:push": "bunx prisma db push",
    "prisma:seed": "bun run prisma/seed.ts",
    "db:reset": "bunx prisma db push --force-reset",
    "db:setup": "bun run prisma:generate && bun run prisma:push"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "@types/bcrypt": "^6.0.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "i": "^0.3.7"
  },
  "devDependencies": {
    "prisma": "^6.19.2",
    "@repo/env-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
